{
  "name": "dutchbrat-app",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix server && npm run build --prefix server",
    "start": "node dist/server.js"
    },
  "dependencies": {
    "@notionhq/client": "^4.0.1",
    "dotenv": "^17.2.0",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2",
    "next": "14.1.3",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "engines": {
    "node": "20.x"
  }
}
